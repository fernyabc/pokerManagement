name: pokerManagement
options:
  bundleIdPrefix: com.pokermanagement
packages:
  WebRTC:
    url: https://github.com/stasel/WebRTC
    from: "140.0.0"
  Starscream:
    url: https://github.com/daltoniam/Starscream
    from: "4.0.0"
targets:
  pokerManagement:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources: [
        "pokerManagement",
        "pokerManagement/Views",
        "pokerManagement/Models",
        "pokerManagement/Services",
        "pokerManagement/Services/LiveActivityManager.swift"
    ]
    dependencies:
      - package: WebRTC
      - package: Starscream
      - target: PokerWidgetExtension
    info:
      path: pokerManagement/Info.plist
      properties:
        UILaunchScreen: {}
        NSSupportsLiveActivities: YES
        NSCameraUsageDescription: "Camera access is needed to capture the poker table when smart glasses are not connected."
        NSMicrophoneUsageDescription: "Microphone access is needed to input pot and bet sizes by voice."
        NSSpeechRecognitionUsageDescription: "Speech recognition is used to parse spoken pot and bet amounts."
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
          UISceneConfigurations: {}
  PokerWidgetExtension:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.0"
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.pokermanagement.pokerManagement.PokerWidgetExtension
    sources: [
        "PokerWidget",
        "pokerManagement/Models/PokerSuggestionAttributes.swift"
    ]
    info:
      path: PokerWidget/Info.plist
      properties:
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
